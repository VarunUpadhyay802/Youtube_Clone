/** @type {import('tailwindcss').Config} */
module.exports = {
  //below we are telling tailwind , what files to track , yo don't need to write html, all are js 
  content: ["./src/**/*.js"],
  theme: {
    extend: {},
  },
  plugins: [],
}

